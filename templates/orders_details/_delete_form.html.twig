<form method="post" action="{{ path('app_orders_details_delete', {'orders': orders_detail.orders}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ orders_detail.orders) }}">
    <button class="btn">Delete</button>
</form>
