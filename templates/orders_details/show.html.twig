{% extends 'base.html.twig' %}

{% block title %}OrdersDetails{% endblock %}

{% block body %}
    <h1>OrdersDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Quantity</th>
                <td>{{ orders_detail.quantity }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ orders_detail.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_orders_details_index') }}">back to list</a>

    <a href="{{ path('app_orders_details_edit', {'orders': orders_detail.orders}) }}">edit</a>

    {{ include('orders_details/_delete_form.html.twig') }}
{% endblock %}
